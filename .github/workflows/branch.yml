name: Auto-create Issue Branches
on:
    issues:
        types:
            - opened
jobs:
    create:
        name: Branch
        runs-on: ubuntu-latest
        steps:
            - name: Create Issue Branch
              uses: robvanderleek/create-issue-branch@main
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
